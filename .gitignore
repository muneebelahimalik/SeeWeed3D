# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Dataset / recordings (regenerable or large; keep out of git)
dataset/
batches/
*.mkv
*.svo
*.svo2

# Model weights (huge; download separately, keep out of git)
*.pt
*.safetensors
models/

# OS / editor
.DS_Store
.idea/
.vscode/

# Shell mishaps: a mistyped redirect creates a file named after the flags
# ("tatus --short" from `git s > tatus --short`). One reached a commit.
tatus*
status*
