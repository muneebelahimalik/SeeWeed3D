[1mdiff --git a/seeweed3d/annotation/prelabel_mixed_sam3.py b/seeweed3d/annotation/prelabel_mixed_sam3.py[m
[1mindex 2afcca2..6720a48 100644[m
[1m--- a/seeweed3d/annotation/prelabel_mixed_sam3.py[m
[1m+++ b/seeweed3d/annotation/prelabel_mixed_sam3.py[m
[36m@@ -179,9 +179,9 @@[m [mfrom common.ontology import (CLASSES, PRELABEL_CATEGORY_ID,  # noqa: E402[m
                              PRELABEL_CLASS, prelabel_categories,[m
                              prelabel_cvat_labels)[m
 from common.progress import Progress  # noqa: E402[m
[31m-from common.vegetation import (component_boxes, distance_peaks,  # noqa: E402[m
[31m-                               excess_green, remove_small, vegetation_mask,[m
[31m-                               vegetation_score, white_balance)[m
[32m+[m[32mfrom common.vegetation import (component_boxes, excess_green,  # noqa: E402[m
[32m+[m[32m                               remove_small, vegetation_mask, vegetation_score,[m
[32m+[m[32m                               white_balance)[m
 from perception.segmenter import drop_fragments  # noqa: E402[m
 [m
 # #############################################################################[m
[36m@@ -204,7 +204,7 @@[m [mCONFIG = {[m
     "OUTPUT_SUBDIR":  "auto_labels_mixed",[m
 [m
     # Mixed sessions. Empty = every session under sessions/.[m
[31m-    "ONLY_SESSIONS": [],[m
[32m+[m[32m    "ONLY_SESSIONS": ["Visit1_20250221_142227","Visit1_20250221_142905","Visit1_20250221_143322"],[m
 [m
     "CVAT_READY_SUBDIR":  "cvat_ready",[m
     "FLAGGED_RGB_SUBDIR": "flagged_rgb",[m
[36m@@ -304,60 +304,11 @@[m [mCONFIG = {[m
     "SEED_ERODE_PX": 2,[m
     "SEED_ERODE_MIN_AREA_PX": 300,[m
 [m
[31m-    # -- Splitting an under-seeded component -----------------------------------[m
[31m-    # When several plants have grown into one another their canopies are ONE[m
[31m-    # connected vegetation component. If SAM proposed a mask for only one of[m
[31m-    # them, the watershed hands that single marker the whole group and four[m
[31m-    # rosettes emerge as one instance - the `a seed grew 91x` line in[m
[31m-    # review_first.txt. peak_seeds() adds a marker at each distance-transform[m
[31m-    # crown so the flood can separate them.[m
[31m-    #[m
[31m-    # ONLY components whose flood would exceed SPLIT_GROWTH_TRIGGER are touched,[m
[31m-    # so a component SAM seeded properly is never re-seeded and cannot be[m
[31m-    # fragmented. That trigger is the same ratio review_reasons() prints, so the[m
[31m-    # symptom and the fix are denominated in the same units: if the review file[m
[31m-    # says a seed grew 20x and the trigger is 8, this now acts on it.[m
[31m-    #[m
[31m-    # Raise SPLIT_GROWTH_TRIGGER if single plants are coming apart; lower it if[m
[31m-    # merges survive. Set SPLIT_UNDERSEEDED False to restore the previous[m
[31m-    # behaviour, where only SAM could ever separate two plants.[m
[31m-    "SPLIT_UNDERSEEDED": True,[m
[31m-    "SPLIT_GROWTH_TRIGGER": 8.0,        # matches REVIEW_MAX_GROWTH[m
[31m-    "SPLIT_MIN_COMPONENT_PX": 2000,     # a group of plants, not one seedling[m
[31m-    "SPLIT_PEAK_REL_THRESHOLD": 0.5,    # a peak this fraction of the largest[m
[31m-    "SPLIT_PEAK_MIN_SEPARATION_PX": 25,[m
[31m-    # THE SETTING THAT STOPS THIS SHATTERING A LEAF. A long leaf has a FLAT[m
[31m-    # distance ridge - every point along a ribbon of constant width has the[m
[31m-    # same inscribed radius - so height alone accepts a dozen "crowns" strung[m
[31m-    # along one onion leaf. Measured on a synthetic 240px ribbon: 10 peaks[m
[31m-    # without this test, 1 with it, while two rosettes joined by a thin neck[m
[31m-    # still give 2. Lower it (towards 0) to split more eagerly; 0 disables the[m
[31m-    # test and restores the behaviour that fragments leaves.[m
[31m-    "SPLIT_PEAK_SADDLE_DROP": 0.7,[m
[31m-    # Component area divided by the area of its own largest inscribed disc.[m
[31m-    # ~1 = one compact plant, ~4 = four merged rosettes, >20 = a long leaf.[m
[31m-    # Above this the component is a ribbon and is never re-seeded, which is[m
[31m-    # what keeps this off onion leaves entirely.[m
[31m-    "SPLIT_MAX_SPREAD": 8.0,[m
[31m-    "SPLIT_SEED_RADIUS_FRAC": 0.55,     # marker as a fraction of inscribed radius[m
[31m-[m
     # -- Recall backstop -------------------------------------------------------[m
     # Vegetation components holding no seed at all. ON here, unlike the weed[m
     # prelabeler - see the module docstring for why the reasoning differs.[m
[31m-    #[m
[31m-    # RECOVER_MIN_AREA_PX IS THE SPECKLE CONTROL, AND ITS RIGHT VALUE DEPENDS ON[m
[31m-    # YOUR MOUNT HEIGHT. On a pale, pebbly substrate a colour index cannot tell[m
[31m-    # green-tinted mineral from a cotyledon - that is why the weed prelabeler[m
[31m-    # ships its own backstop OFF - so size is the discriminator that is left.[m
[31m-    # Gravel on a 1080p frame runs to a few hundred px^2; the smallest real[m
[31m-    # plant is usually several thousand. Run with LOG_INSTANCE_AREAS to see both[m
[31m-    # populations for YOUR imagery and put the floor in the gap between them.[m
[31m-    #[m
[31m-    # RECOVER_MIN_VEG_SCORE is a much weaker filter than it looks: the score[m
[31m-    # saturates at 1.0 for anything comfortably past the ExG threshold, so an[m
[31m-    # olive pebble and a leaf both score ~1.0. Do not rely on it alone.[m
     "RECOVER_UNSEEDED": True,[m
[31m-    "RECOVER_MIN_AREA_PX": 600,[m
[32m+[m[32m    "RECOVER_MIN_AREA_PX": 150,[m
     "RECOVER_MIN_VEG_SCORE": 0.90,[m
 [m
     # -- Instance cleanup ------------------------------------------------------[m
[36m@@ -365,13 +316,7 @@[m [mCONFIG = {[m
     # second true lobe is not. An absolute floor deletes cotyledons.[m
     "FRAGMENT_MIN_FRAC": 0.15,[m
     "FILL_HOLES_MAX_PX": 400,[m
[31m-    # The last gate before an instance reaches CVAT. Every instance under this[m
[31m-    # is one shape somebody has to select and delete by hand, and on pebbly[m
[31m-    # ground there can be hundreds per frame. 250 is the value the weed[m
[31m-    # prelabeler uses on this same imagery without producing speckle; this path[m
[31m-    # ran at 120 and did. Calibrate with LOG_INSTANCE_AREAS rather than guessing[m
[31m-    # - a cotyledon must survive it.[m
[31m-    "MIN_INSTANCE_AREA_PX": 250,[m
[32m+[m[32m    "MIN_INSTANCE_AREA_PX": 120,[m
 [m
     # -- Frame-level safety ----------------------------------------------------[m
     "MAX_VEG_FRACTION": 0.5,          # more green than this = colour cast/glare[m
[36m@@ -417,12 +362,6 @@[m [mCONFIG = {[m
     # calling an onion a weed is the worst error this project can make. Give[m
     # the ambiguous stretch to prelabel_mixed_sam3.py, or leave it out.[m
     "ONLY_FRAMES": {},[m
[31m-    # Print the distribution of instance areas at the end of a run. The floors[m
[31m-    # above are the only thing separating a cotyledon from a pebble, and their[m
[31m-    # right values depend on your mount height - so read them off your own[m
[31m-    # imagery instead of inheriting a number measured on somebody else's.[m
[31m-    "LOG_INSTANCE_AREAS": True,[m
[31m-[m
     "SAVE_PREVIEWS": True,[m
     "PREVIEW_SCALE": 0.5,[m
 }[m
[36m@@ -662,115 +601,6 @@[m [mdef unseeded_components(veg, seeds, cfg, score):[m
     return out[m
 [m
 [m
[31m-def _crown_markers(comp, area, claimed, cfg):[m
[31m-    """Markers at each distinct crown of one vegetation component, or [].[m
[31m-[m
[31m-    ELONGATION IS THE GUARD THAT MAKES THIS SAFE ON ONION. Compare the[m
[31m-    component's area with the area of its own largest inscribed disc: a single[m
[31m-    rosette covers about one such disc, two merged rosettes about two, four[m
[31m-    about four - but a long thin leaf covers dozens, because its inscribed disc[m
[31m-    is only as wide as the leaf. Measured on the synthetic cases: one rosette[m
[31m-    1.0, two merged 2.1, four merged 4.1, one curved onion leaf 27.3. Above the[m
[31m-    ceiling the component is a ribbon, and a ribbon has a FLAT distance ridge[m
[31m-    whose maxima are an artefact of its own irregular edge rather than evidence[m
[31m-    of separate plants - splitting there shattered a synthetic leaf into 11[m
[31m-    instances before this guard existed.[m
[31m-[m
[31m-    The cost is that two merged ONIONS are not split either: two ribbons are[m
[31m-    still a ribbon. That is the safe direction. A missed split leaves one shape[m
[31m-    to divide by hand; a false split teaches the model that a fragment of a[m
[31m-    leaf is a whole plant, which is the failure that closed this door in[m
[31m-    prelabel_weeds_sam3."""[m
[31m-    dt = cv2.distanceTransform(comp.astype(np.uint8), cv2.DIST_L2, 5)[m
[31m-    rmax = float(dt.max())[m
[31m-    if rmax <= 0 or area / (np.pi * rmax * rmax) > cfg["SPLIT_MAX_SPREAD"]:[m
[31m-        return [][m
[31m-    peaks = distance_peaks(comp, cfg["SPLIT_PEAK_REL_THRESHOLD"],[m
[31m-                           cfg["SPLIT_PEAK_MIN_SEPARATION_PX"],[m
[31m-                           min_saddle_drop=cfg["SPLIT_PEAK_SADDLE_DROP"])[m
[31m-    if len(peaks) < 2:[m
[31m-        return []                       # one plant, however large - leave it[m
[31m-    out = [][m
[31m-    for (px, py), radius in peaks:[m
[31m-        if claimed[py, px]:[m
[31m-            continue                    # SAM already owns this crown[m
[31m-        disc = np.zeros(comp.shape, np.uint8)[m
[31m-        cv2.circle(disc, (int(px), int(py)),[m
[31m-                   max(1, int(radius * cfg["SPLIT_SEED_RADIUS_FRAC"])), 1, -1)[m
[31m-        marker = disc.astype(bool) & comp[m
[31m-        if marker.any():[m
[31m-            out.append(marker)[m
[31m-            claimed |= marker[m
[31m-    return out[m
[31m-[m
[31m-[m
[31m-def peak_seeds(veg, seeds, cfg):[m
[31m-    """Extra markers for vegetation components the SAM seeds cannot separate.[m
[31m-[m
[31m-    THE MERGE THIS FIXES. The watershed gives every pixel of a connected[m
[31m-    vegetation component to the nearest seed. When several plants have grown[m
[31m-    into one another their canopies are ONE component, so if SAM proposed a[m
[31m-    mask for only one of them, that single marker inherits the whole group -[m
[31m-    four rosettes emerge as one instance, and the QA line reports it as `a seed[m
[31m-    grew 91x`.[m
[31m-[m
[31m-    The watershed is not the problem and needs no change; it was simply given[m
[31m-    one marker for a region containing four plants. So the fix is more markers,[m
[31m-    not a different partition - and markers are the safe place to intervene. A[m
[31m-    doubtful extra peak produces two markers that the flood may divide almost[m
[31m-    evenly, whereas cutting a FINISHED mask in half on the same evidence is[m
[31m-    what made splitting untenable in the weed prelabeler: there, a leaf[m
[31m-    reaching away from the crown produced a second peak and severed one plant.[m
[31m-[m
[31m-    TRIGGERED BY THE MEASUREMENT, NOT APPLIED EVERYWHERE. Only components whose[m
[31m-    flood would exceed SPLIT_GROWTH_TRIGGER are re-seeded, which is the same[m
[31m-    ratio review_reasons() already prints. A component SAM seeded properly is[m
[31m-    never touched, so this cannot fragment a plant that was already correct.[m
[31m-    """[m
[31m-    if not cfg.get("SPLIT_UNDERSEEDED", True):[m
[31m-        return [][m
[31m-    claimed = np.zeros(veg.shape, bool)[m
[31m-    for s in seeds:[m
[31m-        claimed |= s[m
[31m-[m
[31m-    n, labels, stats, _ = cv2.connectedComponentsWithStats(veg.astype(np.uint8), 8)[m
[31m-    out = [][m
[31m-    for i in range(1, n):[m
[31m-        area = int(stats[i, cv2.CC_STAT_AREA])[m
[31m-        if area < cfg["SPLIT_MIN_COMPONENT_PX"]:[m
[31m-            continue[m
[31m-        comp = labels == i[m
[31m-        seeded_px = int((comp & claimed).sum())[m
[31m-        # A component with NO seed is the recall backstop's business, not this[m
[31m-        # one's. Creating instances there from the vegetation prior alone is[m
[31m-        # exactly what RECOVER_UNSEEDED governs, and quietly doing it here[m
[31m-        # would make that switch a lie. Such components are split, if they[m
[31m-        # deserve it, in split_recovered() - which only runs when the backstop[m
[31m-        # is on.[m
[31m-        if not seeded_px or area / seeded_px <= cfg["SPLIT_GROWTH_TRIGGER"]:[m
[31m-            continue[m
[31m-        for marker in _crown_markers(comp, area, claimed, cfg):[m
[31m-            out.append(marker)[m
[31m-    return out[m
[31m-[m
[31m-[m
[31m-def split_recovered(blobs, cfg):[m
[31m-    """Turn a recovered blob that holds several crowns into one marker each.[m
[31m-[m
[31m-    The recall backstop returns whole unseeded vegetation components, so a[m
[31m-    group of merged plants SAM missed entirely arrives as ONE instance. Same[m
[31m-    evidence and same guards as peak_seeds - it is the same question asked one[m
[31m-    step later, about a component that had no seed rather than too few."""[m
[31m-    if not cfg.get("SPLIT_UNDERSEEDED", True):[m
[31m-        return list(blobs)[m
[31m-    out = [][m
[31m-    for comp in blobs:[m
[31m-        area = int(comp.sum())[m
[31m-        markers = _crown_markers(comp, area, np.zeros(comp.shape, bool), cfg)[m
[31m-        out.extend(markers if len(markers) >= 2 else [comp])[m
[31m-    return out[m
[31m-[m
[31m-[m
 # --------------------------------------------------------------------------- #[m
 # The watershed that actually produces the masks[m
 # --------------------------------------------------------------------------- #[m
[36m@@ -903,16 +733,10 @@[m [mdef analyze_frame(bgr, sam_masks, cfg):[m
     veg = plant_pixels(bgr, cfg)[m
     score = plant_confidence(bgr, veg, cfg)[m
     seeds = seed_masks(sam_masks, veg, cfg)[m
[31m-    # Extra markers BEFORE the recall backstop, so a group of merged plants is[m
[31m-    # split into one instance each rather than recovered as a single blob.[m
[31m-    peaks = peak_seeds(veg, seeds, cfg)[m
[31m-    recovered = split_recovered([m
[31m-        unseeded_components(veg, seeds + peaks, cfg, score), cfg)[m
[31m-    sources = (["sam"] * len(seeds) + ["peak"] * len(peaks)[m
[31m-               + ["vegetation"] * len(recovered))[m
[31m-[m
[31m-    allseeds = ([erode_seed(s, cfg) for s in seeds] + list(peaks)[m
[31m-                + list(recovered))[m
[32m+[m[32m    recovered = unseeded_components(veg, seeds, cfg, score)[m
[32m+[m[32m    sources = ["sam"] * len(seeds) + ["vegetation"] * len(recovered)[m
[32m+[m
[32m+[m[32m    allseeds = [erode_seed(s, cfg) for s in seeds] + list(recovered)[m
     seed_px = [int(s.sum()) for s in allseeds][m
     grown = partition_vegetation(veg, allseeds)[m
 [m
[36m@@ -941,7 +765,6 @@[m [mdef analyze_frame(bgr, sam_masks, cfg):[m
         "instances": len(instances),[m
         "from_sam": sum(1 for i in instances if i["source"] == "sam"),[m
         "recovered": sum(1 for i in instances if i["source"] == "vegetation"),[m
[31m-        "split": sum(1 for i in instances if i["source"] == "peak"),[m
         "veg_px": veg_px,[m
         # The number to watch. Vegetation outside every instance is plant an[m
         # annotator will never be shown, and in a prelabelled task nobody draws[m
[36m@@ -954,29 +777,6 @@[m [mdef analyze_frame(bgr, sam_masks, cfg):[m
     return instances, veg, qa[m
 [m
 [m
[31m-def area_histogram(areas, cfg):[m
[31m-    """Where the size floors sit relative to the instances actually produced.[m
[31m-[m
[31m-    On a pale, pebbly substrate a colour index cannot separate green-tinted[m
[31m-    mineral from a cotyledon, so SIZE is the only discriminator left - and the[m
[31m-    right floor depends on mount height, which is a property of your rig and[m
[31m-    not of this code. Real plants and gravel are usually an order of magnitude[m
[31m-    apart, so the two populations are obvious once printed. Put the floor in[m
[31m-    the gap between them rather than inheriting a number measured elsewhere."""[m
[31m-    a = sorted(int(v) for v in areas)[m
[31m-    if not a:[m
[31m-        return "no instances"[m
[31m-    def pct(p):[m
[31m-        return a[min(len(a) - 1, int(p * (len(a) - 1)))][m
[31m-    floor = int(cfg.get("MIN_INSTANCE_AREA_PX", 0))[m
[31m-    tiny = sum(1 for v in a if v < floor * 4)[m
[31m-    return (f"instance area px: p10={pct(0.10)} p50={pct(0.50)} "[m
[31m-            f"p90={pct(0.90)} max={a[-1]} | {tiny} of {len(a)} "[m
[31m-            f"({tiny / len(a):.0%}) under 4x the {floor}px floor"[m
[31m-            + (" - suspect speckle, raise MIN_INSTANCE_AREA_PX and "[m
[31m-               "RECOVER_MIN_AREA_PX" if tiny > len(a) * 0.5 else ""))[m
[31m-[m
[31m-[m
 def review_reasons(qa, cfg):[m
     """Why this frame should be corrected before the others. Empty = looks fine.[m
 [m
[36m@@ -1057,7 +857,7 @@[m [mdef overlay(bgr, instances, scale):[m
                            cv2.COLOR_HSV2BGR)[0, 0].tolist()[m
         cnts, _ = cv2.findContours(inst["mask"].astype(np.uint8),[m
                                    cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)[m
[31m-        if inst["source"] != "sam":[m
[32m+[m[32m        if inst["source"] == "vegetation":[m
             cv2.drawContours(vis, cnts, -1, (255, 255, 255), 4)[m
         cv2.drawContours(vis, cnts, -1, col, 2)[m
     return cv2.resize(vis, None, fx=scale, fy=scale,[m
[36m@@ -1098,8 +898,7 @@[m [mdef prelabel_session(sid, session_dir, out_root, cfg, predictor, sam_fn):[m
 [m
     coco, rows, flagged, review = PrelabelCoco(), [], [], [][m
     stats = {"frames": 0, "instances": 0, "flagged": 0, "empty": 0,[m
[31m-             "recovered": 0, "split": 0, "veg_px": 0, "veg_covered_px": 0}[m
[31m-    areas = [][m
[32m+[m[32m             "recovered": 0, "veg_px": 0, "veg_covered_px": 0}[m
 [m
     prog = Progress(len(frames), f"[{sid}]", unit="frames")[m
     for fn in frames:[m
[36m@@ -1161,8 +960,6 @@[m [mdef prelabel_session(sid, session_dir, out_root, cfg, predictor, sam_fn):[m
         stats["instances"] += len(instances)[m
         stats["empty"] += int(not instances)[m
         stats["recovered"] += qa["recovered"][m
[31m-        stats["split"] += qa.get("split", 0)[m
[31m-        areas.extend(i["area_px"] for i in instances)[m
         stats["veg_px"] += qa["veg_px"][m
         stats["veg_covered_px"] += int((veg & union).sum())[m
         prog.update(note=f"{stats['instances']} inst, {stats['flagged']} flagged")[m
[36m@@ -1191,11 +988,7 @@[m [mdef prelabel_session(sid, session_dir, out_root, cfg, predictor, sam_fn):[m
         # Coverage, not instance count, is the honest recall readout: the[m
         # pipeline can look productive while quietly leaving plants out.[m
         print(f"      coverage: {cov:.1f}% of vegetation inside an instance "[m
[31m-              f"| {sam_n - stats['split']} from SAM "[m
[31m-              f"+ {stats['split']} split from a merge "[m
[31m-              f"+ {stats['recovered']} recovered")[m
[31m-    if cfg.get("LOG_INSTANCE_AREAS", True) and areas:[m
[31m-        print(f"      {area_histogram(areas, cfg)}")[m
[32m+[m[32m              f"| {sam_n} from SAM + {stats['recovered']} recovered")[m
     if review:[m
         print(f"      {len(review)} frame(s) to review first -> review_first.txt")[m
     print(f"      -> {out}")[m
